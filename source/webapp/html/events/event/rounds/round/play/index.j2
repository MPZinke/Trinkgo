<!DOCTYPE html>
<html>
	<title>
		Play {{ round.name }}
	</title>

	<body>
		<header>
			<a
				href="/"
			>
				⌂
			</a>
			&gt;
			<a
				href="/events"
			>
				Events
			</a>
			&gt;
			<a
				href="/events/{{ round.event.id }}"
			>
				{{ round.event.name }}
			</a>
			&gt;
			<a
				href="/events/{{ round.event.id }}/rounds"
			>
				Rounds
			</a>
			&gt;
			<a
				href="/events/{{ round.event.id }}/rounds/{{ round.event.id }}"
			>
				{{ round.name }}
			</a>
			&gt; Play
		</header>

		<h1>
			Play {{ round.name }}
		</h1>

		<table>
			<tr>
				<td>
					Playlist Set
				</td>
				<td>
					<a
						href="/playlists/{{ round.playlist_set.playlist.id }}/sets/{{ round.playlist_set.id }}"
					>
						{{ round.playlist_set.name }}
					</a>
				</td>
			</tr>
			<tr>
				<td>
					Size
				</td>
				<td>
					{{ round.size }}
				</td>
			</tr>
			<tr>
				<td>
					Started
				</td>
				<td>
					{{ round.start }}
				</td>
			</tr>
			<tr>
				<td>
					Ended
				</td>
				<td>
					{{ round.ended }}
				</td>
			</tr>
		</table>

		<h2>
			<a
				href="/events/{{ round.event.id }}/rounds/{{ round.id }}/cards"
			>
				Cards ({{ round.cards | length }})
			</a>
		</h2>

		<table>
			<tr>
				{# TODO: make scrollable #}
				<td
					style="vertical-align: top;"
					width="50%"
				>
					<h2>
						Played
					</h2>
					<table>
						{% for played_set_song in round.played_set_song %}
							{% set index=loop.index %}
							{% include "events/event/rounds/round/play/_played_song.j2" with context %}
						{% endfor %}
					</table>
				</td>

				<td
					style="vertical-align: top;"
					width="50%"
				>
				<div style="overflow-y: scroll;">
					<h2>
						Upcoming
					</h2>

					<table>
						{% for set_song in round.playlist_set.set_songs %}
							{% if song not in round.calls %}
								{% set index=loop.index %}
								{% include "events/event/rounds/round/play/_unplayed_song.j2" with context %}
							{% endif %}
						{% endfor %}
					</table>
				</div>
				</td>
			</tr>
		</table>
	</body>
</html>

{% include("player.j2") %}

<script>
	function play_song_next()
</script>
